buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.3.2'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
/*
dependencies {
    classpath 'androidx.webkit:webkit:1.0.0'
    classpath 'androidx.browser:browser:1.0.0'
    classpath 'androidx.appcompat:appcompat:1.0.0'
    classpath 'com.squareup.okhttp3:mockwebserver:3.11.0'
}*/
